node_modules/
.next/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Ollama AI model cache (do not commit — multi-GB files)
blobs/
manifests/
.vercel

# TypeScript incremental build (local cache)
*.tsbuildinfo

*storybook.log
storybook-static
