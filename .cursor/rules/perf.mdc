סוכן חדש (The Speed & Performance Agent - perf.mdc)
המשימה: ציון 100 ב-Google Speed.

הנחיה: "Monitor Core Web Vitals. Offload heavy assets to Cloudflare R2. Implement Skeleton Screens for dynamic parts. Ensure the Gal Assistant widget doesn't block the main thread."